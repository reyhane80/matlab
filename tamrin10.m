% ماتریس نمرات دانشجویان
a = [16 15 17;12 11 20;17 18 20;18 19 11;10 9 5;2 4 20];

% ماتریس واحد دروس
b = (2:4);

% به دست آوردن تعداد دانشجویان
d=a(:);
e = size(d,1);

% محاسبه معدل هر دانشجو
f =((a(:,1)*2)+(a(:,2)*3)+(a(:,3)*4))/sum(b);

% محاسبه معدل هر درس
g = [sum(a(:,1)),sum(a(:,2)),sum(a(:,3))]/size(a,1);

% معدل کل دروس برای تمامی دانشجویان
h = sum(f)/size(a,1);

% میانگین نمرات زیر ده بدون احتساب واحد درس
i = sum(sum(a.*(a <= 10)))/sum(sum((a.*(a <= 10))~= 0));